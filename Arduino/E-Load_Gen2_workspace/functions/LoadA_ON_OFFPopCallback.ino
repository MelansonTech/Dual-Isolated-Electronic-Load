void LoadA_ON_OFFPopCallback(void *ptr)  // Release event for dual state button bt0
{
//  uint32_t BTstate = 0; // temp var to hold state value of button  
//  LoadA_ON_OFF.getValue(&BTstate);  // Read value of dual state button to know the state (0 or 1)
//  DBserial.println("Load state is");  
//  DBserial.println(BTstate);   
  
//  Wire.beginTransmission(LDA_ADD); //U1B
//  Wire.write(BTstate);   // sends instruction byte 
//  Wire.endTransmission();     // stop transmitting
    
   
   
}  // End of release event
